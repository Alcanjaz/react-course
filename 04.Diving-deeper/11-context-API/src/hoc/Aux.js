//We can use an auxiliar element to wrap our components into it
//it looks tricky but is very useful to return multiple divs on our component
const aux = props => props.children;    

export default aux;